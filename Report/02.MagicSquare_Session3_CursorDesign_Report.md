# MagicSquare_xx — 세션 3 Cursor 설계 보고서 (02)

> 작성 기준: 세션 3 워크북 · Cursor 8계층 + Hook  
> 선행 문서: [01.MagicSquare_ProblemDefinition_Report.md](01.MagicSquare_ProblemDefinition_Report.md)  
> Transcript: [Prompting/02.MagicSquare_Session3_CursorDesign-Transcript.md](../Prompting/02.MagicSquare_Session3_CursorDesign-Transcript.md)

---

## STEP 1 — 주제 (1문장)

**부분 마방진(4×4, 빈칸 2개)을 풀 때 10선 판정을 빠짐없이 확인하지 못해 시간을 낭비하는 문제를, Cursor 규칙·커맨드·테스트 루프로 "빠진 검증을 바로 드러나게" 만든다.**

(Mom Test · Report/01 STEP 3과 동일)

---

## STEP 2 — 세션 3 산출물 목록

| 계층 | 산출물 | 경로 | 상태 |
|------|--------|------|------|
| **Rule** | `.cursorrules` | 루트 | ✅ (76줄, 초안) |
| **Harness** | pytest + ECB 골격 | `pyproject.toml`, `src/`, `tests/` | ✅ |
| **Git** | 원격 브랜치 동기화 | `origin/main`, `origin/spec`, `origin/staging` | ✅ |
| **Skill** | `magic-square-tdd` | `.cursor/skills/magic-square-tdd/` | ❌ 미작성 |
| **Command** | `/tdd-red`, `/review-ecb` | `.cursor/commands/` | ❌ 미작성 |
| **Hook** | sessionStart, postToolUse | `.cursor/hooks.json` | ❌ 미작성 |
| **Test Loop** | RED pytest FAIL | `tests/entity/test_d_*.py` | ❌ 미작성 |

---

## STEP 3 — Harness (ECB + Dual-Track)

### 3.1 디렉터리 구조

```
MagicSquare_xx/
├── pyproject.toml
├── src/
│   ├── boundary/__init__.py
│   ├── control/__init__.py
│   └── entity/__init__.py
└── tests/
    ├── boundary/__init__.py
    ├── control/__init__.py
    └── entity/__init__.py
```

### 3.2 `pyproject.toml` 요약

| 항목 | 값 |
|------|-----|
| 프로젝트명 | `magic-square-xx` |
| Python | `>=3.10` |
| dev 의존성 | `pytest>=8.0` |
| `testpaths` | `["tests"]` |
| `pythonpath` | `["src"]` |

### 3.3 검증

```bash
python -m pip install -e ".[dev]"
python -m pytest --collect-only
```

| 결과 | 의미 |
|------|------|
| `no tests collected` (exit 5) | 테스트 파일 없음 — Harness 골격만 존재, **정상** |

### 3.4 Dual-Track 대응 (앞으로 채울 때)

| Track | `src/` | `tests/` | Mock |
|-------|--------|----------|------|
| Logic | `entity/`, `control/` | `tests/entity/`, `tests/control/` | Domain Mock **금지** |
| UI | `boundary/` | `tests/boundary/` | Mock **허용** |

---

## STEP 4 — `.cursorrules` 요약

| 영역 | 핵심 |
|------|------|
| SSOT | `docs/PRD.md` > `Report/*` > `.cursorrules` |
| 도메인 | 4×4, 빈칸 0×2, 1~16, **10선=마방진 상수 34**, 출력 `int[6]` 1-index |
| Mom Test | 행·열만 검증하고 `\`·`/` 누락 금지 |
| 오류 | boundary E001~E007, entity E001~E005 **emit 금지**, E006/E007 entity→boundary 매핑 |
| ECB | boundary→control→entity, entity 역방향 import 금지 |
| Dual-Track | Logic Mock 금지 / UI Mock 허용, `D-*`/`U-*`, `test_d_*`/`test_u_*` |
| TDD | RED→GREEN→REFACTOR, skip·xfail·assert 완화 금지, MagicConstant SSOT |
| AI | 한국어, Phase/Layer/Track 선언, commit은 요청 시만 |

### entity vs E004 (역할 분리)

- **entity:** 10선 판정·풀이 **로직** 수행
- **boundary/control:** E004 **코드 부여·반환** — PRD FR-VAL과 `.cursorrules` 모순 방지

---

## STEP 5 — GitHub 동기화

| 브랜치 | 원격 | 커밋 | 비고 |
|--------|------|------|------|
| `main` | `origin/main` | `3452205` | Harness 커밋 포함 |
| `spec` | `origin/spec` | `3452205` | 신규 푸시 |
| `staging` | `origin/staging` | `3452205` | 신규 푸시 |

**저장소:** https://github.com/Bang-Jemin/MagicSquare_xx

| 커밋 | 내용 |
|------|------|
| `fceb105` | Mom Test 문제 정의, PRD, 세션 문서 |
| `3452205` | ECB + Dual-Track TDD pytest harness skeleton |

> `.cursorrules`는 세션 3 작업 중 **로컬 생성·미커밋** 상태 (Report 02 작성 시점).

---

## STEP 6 — 8계층 + Hook 점검

| 계층 | 상태 | 근거 |
|------|------|------|
| **Model** | ⚠️ | Cursor 기본 LLM. 프로젝트 전용 Model 설정 없음 |
| **Agent** | ⚠️ | RED/GREEN Phase별 수정 범위 Rule에만 명시. Command 미작성 |
| **Harness** | ✅ | `pyproject.toml` + ECB `src/`·`tests/` 골격, pytest collect OK |
| **Rule** | ✅ | `.cursorrules` 76줄 — 도메인·ECB·Dual-Track·TDD·AI 행동 |
| **Skill** | ❌ | `magic-square-tdd` 미작성 |
| **Command** | ❌ | `/tdd-red`, `/review-ecb` 미작성 |
| **Tool/MCP** | ⚠️ | Shell(pytest) 전제. RED FAIL 로그 산출물 없음 |
| **Test/Review Loop** | ❌ | `test_d_*` 0개. Mom Test SC-1~2 **pytest 미증명** |
| **Hook** | ❌ | `hooks.json` 없음 |

---

## STEP 7 — Test/Review Loop 성공 기준 (세션 3)

| ID | 기준 | 검증 | 상태 |
|----|------|------|------|
| SC-L1 | Rule 존재 | `.cursorrules` 파일·키워드 | ✅ |
| SC-L2 | Harness pytest | `python -m pytest --collect-only` | ✅ |
| SC-L3 | `/tdd-red` 절차 | Phase 선언·tests/만·FAIL 보고 | ❌ (Command 없음) |
| SC-L4 | pytest RED FAIL | `test_d_val_03` 등 FAIL | ❌ |
| SC-L5 | `/review-ecb` | P0 위반 표 | ❌ (Command 없음) |

---

## STEP 8 — 세션 3 범위 대비

### In (완료)

- Harness ECB 골격 (`pyproject.toml`, `src/`, `tests/`)
- Rule (`.cursorrules` 초안)
- GitHub 3브랜치 동기화 (`main`, `spec`, `staging`)
- Report 02 · Prompting 02 (본 문서)

### Out (의도적 미포함 · 미완)

- Skill · Command · Hook
- **RED 테스트 1개 + pytest FAIL 증거** ← Loop **미완**
- Domain GREEN 구현
- PyQt UI
- `.cursorrules` git commit (사용자 요청 시)

---

## STEP 9 — 권고 (1건)

**Test/Review Loop 빈 구멍:** Harness와 Rule은 준비됐지만 **`tests/entity/test_d_val_03.py` RED + pytest FAIL 로그**가 없어 Mom Test(대각선 `\` 누락)가 **기계적으로 재현되지 않음**. 세션 4 첫 작업으로 `/tdd-red` Command 작성 → D-VAL-03 RED → FAIL 확인을 권장.

---

## STEP 10 — 다음 세션(4) 제안

1. `.cursorrules` commit (사용자 요청 시)
2. `/tdd-red` Command + (선택) `magic-square-tdd` Skill
3. D-VAL-03 RED (`validate_main_diagonal`) → pytest FAIL 확인
4. D-VAL-04~05 → SC-1~2 pytest 증명
5. (선택) `/review-ecb`, `hooks.json` (pytest 힌트 Hook)

---

## 부록 — 파일 트리 (현재)

```
MagicSquare_xx/
├── .cursorrules                    ← 세션 3 (미커밋)
├── pyproject.toml                  ← 세션 3 ✅
├── README.md
├── docs/PRD.md
├── src/
│   ├── boundary/__init__.py
│   ├── control/__init__.py
│   └── entity/__init__.py
├── tests/
│   ├── boundary/__init__.py
│   ├── control/__init__.py
│   └── entity/__init__.py
├── Report/
│   ├── 01.MagicSquare_ProblemDefinition_Report.md
│   └── 02.MagicSquare_Session3_CursorDesign_Report.md  ← 본 문서
└── Prompting/
    ├── 01.MagicSquare_ProblemDefinition_Report-Promt.md
    └── 02.MagicSquare_Session3_CursorDesign-Transcript.md
```
